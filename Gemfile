source 'https://rubygems.org'

# Define all dependencies in your .gemspec file
gemspec

gem 'rake'
gem 'motion-spec'

group :ios do
  gem 'webstub'
  gem 'redpotion' # for dummy app
end

# Android Only
group :android do
  gem 'motion-gradle'
end
