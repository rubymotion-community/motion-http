source 'https://rubygems.org'

# Define all dependencies in your .gemspec file
gemspec

gem 'rake'

group :ios do
  gem 'motion-spec'
  gem 'webstub'
  gem 'redpotion' # for dummy app
end

# Android Only
group :android do
  gem 'motion-gradle'
end
